<%= form_with(model: csv_filter, local: true) do |form| %>
  <% if csv_filter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(csv_filter.errors.count, "error") %> prohibited this csv_filter from being saved:</h2>

      <ul>
      <% csv_filter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
